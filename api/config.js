/* ============================================================
   api/config.js — Supabase public credentials
   Replace placeholders to activate the contact form.
   The anon key is safe client-side (RLS insert-only policy).
   ============================================================ */
window.SUPABASE_URL      = 'YOUR_SUPABASE_URL';
window.SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
