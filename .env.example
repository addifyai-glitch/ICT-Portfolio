# Copy to .env.local for local dev reference.
# These values go into api/config.js — NOT a server .env
# (this is a fully static site; no server-side env needed)

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
